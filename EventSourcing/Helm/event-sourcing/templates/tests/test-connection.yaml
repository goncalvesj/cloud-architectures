apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "event-sourcing.fullname" . }}-test-connection"
  labels:
    {{- include "event-sourcing.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "event-sourcing.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
